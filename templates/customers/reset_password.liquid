{% comment %} Password Reset Page {% endcomment %}
<div class="customer-page">
  <div class="container">
    <h1>Reinitialiser le Mot de Passe</h1>
    <p style="text-align: center; color: var(--color-text-light); margin-bottom: 32px;">
      Entrez votre nouveau mot de passe.
    </p>

    {% form 'reset_customer_password', class: 'customer-form' %}
      {% if form.errors %}
        <div class="form-errors" style="color: var(--color-accent); margin-bottom: 16px;">
          {{ form.errors | default_errors }}
        </div>
      {% endif %}

      <div class="form-group">
        <label for="password">Nouveau mot de passe</label>
        <input type="password" id="password" name="customer[password]" required>
      </div>
      <div class="form-group">
        <label for="password_confirmation">Confirmer le mot de passe</label>
        <input type="password" id="password_confirmation" name="customer[password_confirmation]" required>
      </div>
      <div class="form-actions">
        <button type="submit" class="btn btn-primary btn-lg" style="width: 100%;">Enregistrer</button>
      </div>
    {% endform %}

    <p class="form-link">
      <a href="{{ routes.account_login_url }}">Retour a la connexion</a>
    </p>
  </div>
</div>
